syntax = "proto3";

package MatchMethod;

enum MatchMethod {
    EXACT_MATCH = 0;
    REGULAR_MATCH = 1;
    NO_FIXED_DATA_MATCH = 2;
    MATCH_MULTI_DATA = 3;
    PART_EXACT_MATCH = 4;
    BM_MATCH_STR = 5;
}
